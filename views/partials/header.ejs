<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>GameHub</title>
  <link rel="stylesheet" href="/css/style.css">

  <% if (locals.currentGame && locals.currentGame.cssFile) { %> 
     <link rel="stylesheet" href="/css/<%= locals.currentGame.cssFile %>">
  <% } %>

</head>
<body>
  <header>
    <h1>GameHub</h1>
    <nav>
      <ul>
        <li><a href="/">Accueil</a></li>
        <% for (let game of locals.games) { %> 
            <li><a href="/game/<%= game.name %>"><%= game.title %></a></li>
        <% } %> 
      </ul>
    </nav>
  </header>
